import React from "react";
import { StyleSheet, Text, View } from "react-native";

const index = () => {
  return <View>{/* <ImagePickerComponent></ImagePickerComponent> */}</View>;
};

export default index;

const styles = StyleSheet.create({});
